# agent/windows

Windows PTY backend — `pywinpty` (wraps ConPTY), required for real
color/cursor support in cmd.exe/PowerShell/claude.exe. Ships as a Windows
service or scheduled task. Not yet implemented.
